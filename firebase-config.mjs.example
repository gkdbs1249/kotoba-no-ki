// Copy this file to firebase-config.mjs and replace every placeholder.
// Firebase web configuration is public metadata; never put service-account keys here.
export const firebaseConfig = Object.freeze({
  apiKey: 'REPLACE_WITH_FIREBASE_WEB_API_KEY',
  authDomain: 'REPLACE_WITH_PROJECT_ID.firebaseapp.com',
  projectId: 'REPLACE_WITH_PROJECT_ID',
  storageBucket: 'REPLACE_WITH_PROJECT_ID.firebasestorage.app',
  messagingSenderId: 'REPLACE_WITH_MESSAGING_SENDER_ID',
  appId: 'REPLACE_WITH_FIREBASE_APP_ID',
});

// Keep this dedicated to Kotoba no Ki. It must not be shared with another app.
export const accountEmailDomain = 'accounts.kotoba-no-ki.invalid';
